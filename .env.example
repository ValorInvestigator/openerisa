# Turso (libSQL) connection — copy to .env.local and fill in.
# Get values:  turso db show erisa --url   /   turso db tokens create erisa
TURSO_DATABASE_URL=libsql://erisa-<org>.turso.io
TURSO_AUTH_TOKEN=
